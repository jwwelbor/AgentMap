# B005 craft code review

## A. Executive summary

B005 adds a provider-neutral `LLMResponse.text_status` receipt field and derives it beside the existing safe text projection, so a successful non-text-only async response is distinguishable from ordinary empty text without exposing raw provider blocks.

The implementation is localized to the data model, receipt normalizer, async receipt construction, and a caller-path regression. Risk is low: the public rich-receipt path is wired correctly and the string-only `ask_async()` contract is unchanged.

**Verdict: PASS-with-triage.** Counts: 0 blockers / 1 non-blocker to triage / 1 nit.

Scope reviewed: `a442f79^..a442f79` (206 additions, 15 deletions) and its production caller surface. No unrelated implementation changes were found; the added B005 research report is task-scoped.

## B. Findings table

| id | severity | file:line | rule | diagnosis | evidence | correction |
|---|---|---|---|---|---|---|
| CR-1 | non-blocker | `tests/fresh_suite/unit/services/llm/test_tool_call_extraction.py:178` | TESTS | The new three-state classifier has no direct boundary tests for `empty` and `text`; existing helper tests assert only its legacy string projection. | `normalize_response_content()` produces `empty` for `""`, `[]`, and an empty text block, and `text` for a non-empty string at `tool_call_extraction.py:111-136`; the B005 caller-path test covers only `non_text` at `test_llm_service_async.py:1454-1493`. | Add table-driven direct tests of `normalize_response_content()` for `("", "empty")`, `([], "empty")`, `([{\"type\": \"text\", \"text\": \"\"}], "empty")`, and `("hello", "text")`, while keeping the existing live caller-path test. |
| CR-2 | nit | `src/agentmap/services/llm/tool_call_extraction.py:4-5` | IDIOM | The module documentation still says there are two helpers used at construction in `_invoke_with_resilience_async`; the new receipt normalizer is a third helper and construction is now in `_attempt_llm_call_async`. | `normalize_response_content()` is defined at `tool_call_extraction.py:104`; it is called at `llm_service.py:1828`. | Update the module overview to name the receipt normalizer, label `normalize_response_text()` as its string-only wrapper, and cite `_attempt_llm_call_async`. |

## C. Reuse opportunities

Reuse search performed with `rg -n "normalize_response_text" --glob '*.py' --glob '!tests/**' .` and `rg -n "ResponseTextStatus|text_status" src tests --glob '*.py'`.

No duplicate production response-normalization helper or receipt-status implementation was found. The existing `normalize_response_text()` at `src/agentmap/services/llm/tool_call_extraction.py:83` is reused as the compatibility projection through the new `normalize_response_content()` implementation rather than being reimplemented.

## D. Standards crosswalk

| finding id | standards doc | section | quoted clause |
|---|---|---|---|
| CR-1 | `claude.md` | `patterns.testing` | “focus on testing business logic and contracts” |
| CR-2 | none | opinion-only | No project standards section specifically governs module-docstring freshness; this is a nit. |

## E. Tests review

The B005 caller-path test, `TestLLMServiceToolCallAndTextNormalizationWiring.test_b005_tool_use_only_response_is_explicitly_non_text` at `tests/fresh_suite/unit/services/test_llm_service_async.py:1454`, uses the allowed lowest mock seam (client factory) and invokes the real normalizer and extractor. It would fail against the prior buggy implementation because `LLMResponse` had neither `text_status` nor a non-text classification. It also confirms raw provider text remains the safe empty-string projection and normalized tool calls remain intact.

No test commands were run in this craft-review phase; test execution belongs to QA. The review examined test design only.

Missing boundary coverage is CR-1. The test suite has no timing, random-data, network, or shared-state additions in this diff. The client factory seam is appropriate for a service test per `claude.md` `patterns.testing`.

### Counter-factual table

| AC / contract | covering test | would it fail against the prior buggy implementation? |
|---|---|---|
| Successful non-text-only async receipt is distinguishable from ordinary empty text | `test_b005_tool_use_only_response_is_explicitly_non_text` at `test_llm_service_async.py:1454-1493` | Yes. The prior `LLMResponse` lacks `text_status`; an empty/legacy implementation cannot satisfy the asserted `"non_text"` receipt outcome. |
| Non-text raw content is not exposed through `.text` and normalized tool calls remain available | Same test at `test_llm_service_async.py:1488-1493` | Yes for the new status + preserved structured receipt combination; a solution that stringified the raw block or omitted the receipt status fails. Tool-call preservation itself pre-existed and is therefore regression coverage, not sole B005 proof. |
| Empty and ordinary text receipts retain their distinct status values | No direct B005 test | Not demonstrated. This is CR-1, a non-blocking contract-coverage gap; add direct normalizer tests. |

## F. Quality rubric

| file | readability | maintainability | performance | testability | standards | notes |
|---|---:|---:|---:|---:|---:|---|
| `src/agentmap/models/llm_execution.py` | 5 | 5 | 5 | 5 | 5 | Data-only, frozen receipt extension with a closed literal type and additive default. |
| `src/agentmap/services/llm/tool_call_extraction.py` | 4 | 4 | 5 | 4 | 4 | One-pass, pure classification is clear and bounded; direct status-boundary tests and docstring refresh would improve it. |
| `src/agentmap/services/llm_service.py` | 5 | 5 | 5 | 5 | 5 | Existing receipt construction consumes the richer normalizer result without changing resilience, telemetry, cost, or tool-call sequence. |
| `tests/fresh_suite/unit/services/test_llm_service_async.py` | 5 | 4 | 5 | 5 | 5 | Appropriate caller-path test; complementary pure-function status boundaries are absent. |

No dimension averages 3 or below.

## G. Risk hotspots

- **I/O / provider boundary — low risk:** the new classifier runs after a successful provider response and does not change invocation, retry, timeout, telemetry, circuit-breaker, usage, cost, or streaming behavior (`llm_service.py:1817-1858`).
- **Sensitive-content boundary — low risk:** non-text raw blocks remain out of `LLMResponse.text` (`tool_call_extraction.py:104-109`), avoiding accidental exposure of tool arguments or reasoning.
- **Contract-boundary coverage — non-blocker:** CR-1 leaves the new `empty`/`text` branches without direct status assertions.

## G2. Codex red-team

Independent Codex craft red-team completed successfully (read-only; session `01a08ecd-4a94-7ea2-824f-e85f5c6f4989`). Its assessment follows verbatim:

> 1. **Non-blocker — status-classification contract lacks direct boundary coverage.** The new end-to-end test proves the target `tool_use` case reaches `call_llm_async()` with `"non_text"` (`test_llm_service_async.py:1454`). But it does not assert the counterfactual states implemented by the new classifier: `""`, `[]`, and `[{"type": "text", "text": ""}]` must be `"empty"`; ordinary non-empty text must be `"text"` (`tool_call_extraction.py:111`). The existing helper tests still exercise only the old string projection (`test_tool_call_extraction.py:178`). Correction: add direct table-driven/unit cases for `normalize_response_content()` covering all three statuses, and retain the current public-path regression. This follows the repository testing standard: “focus on testing business logic and contracts” (`claude.md:244`).
>
> 2. **Nit — module documentation is stale after the refactor.** It says two helpers are used at construction in `_invoke_with_resilience_async` (`tool_call_extraction.py:4`), but the new status-bearing normalizer is a third helper and receipt construction now occurs in `_attempt_llm_call_async` (`llm_service.py:1828`). Correction: describe `normalize_response_content()` as the receipt normalizer and `normalize_response_text()` as its string-only compatibility wrapper.
>
> No blockers. The production chain is sound: `call_llm_async()` returns the enriched `LLMResponse`; routing and fallback re-enter the same resilience seam; `ask_async()` deliberately returns only `response.text` (`llm_service.py:2420`). Therefore callers needing the distinction must use `call_llm_async()`, as intended. No duplicate production normalizer was found.
>
> Recommended: **PASS-with-triage**.

`codex_disagreements`: none. `downgrade_justifications`: none; the Codex classifications are retained unchanged.

## H. Production caller chains

### `LLMResponse.text_status` receipt construction

- **Entrypoint:** `LLMService.call_llm_async()` at `src/agentmap/services/llm_service.py:499`.
- **Primary direct chain:** `call_llm_async()` (`:534-538`) → `_dispatch_call_llm_async()` (`:540`) → `_call_llm_async_core()` / routing dispatch → `_call_llm_async_direct()` (`:1378`) → `_bind_and_invoke_direct()` (`:1260`) → `_invoke_with_resilience_async()` (`:1620`) → `_run_resilient_retry_loop()` → `_attempt_llm_call_async()` (`:1794`) → `normalize_response_content()` (`src/agentmap/services/llm/tool_call_extraction.py:104`) → `LLMResponse(..., text_status=text_status)` (`llm_service.py:1849-1857`).
- **Argument shape:** production callers provide `messages: List[LLMMessage]`, optional `provider`, `model`, `temperature`, routing context, cache flag, tools, and kwargs. The direct path resolves a normalized `provider`, configured `current_model`, real provider `client`, and converted `langchain_messages`; the leaf receives the raw response returned by `client.ainvoke()` and derives `(text, text_status)` from its `content`.
- **Fallback chain:** `LLMService.__init__()` installs `invoke_async_fn=lambda client, msgs, provider, model: _invoke_with_resilience_async(..., attempt_kind="fallback")` at `src/agentmap/services/llm_service.py:225-232`; fallback responses therefore use the same receipt construction leaf.

### String convenience API

- **Entrypoint:** `LLMService.ask_async()` at `src/agentmap/services/llm_service.py:2420`.
- **Chain:** `ask_async(prompt, provider, **kwargs)` (`:2424-2428`) constructs `[{"role": "user", "content": prompt}]` → `call_llm_async()` chain above → `return response.text` (`:2429`).
- **Argument shape:** the wrapper supplies the default provider `"anthropic"` when absent and returns only a `str`; it deliberately does not surface `text_status`. This preserves its declared public contract, while consumers that require the distinction use `call_llm_async()`.

## I. Triage summary

- **Blockers:** none.
- **Non-blockers to triage:** CR-1 — add direct `normalize_response_content()` status-boundary contract tests. Suggested tech-debt payload: `file_line=tests/fresh_suite/unit/services/llm/test_tool_call_extraction.py:178`, `summary=New LLM receipt status classifier lacks direct empty/text boundary coverage`, `fix_suggestion=Add table-driven unit coverage for all three ResponseTextStatus values while retaining B005 caller-path coverage.`
- **Nits:** CR-2 — refresh the stale module overview for the new normalizer and its actual receipt-construction leaf.

## J. Verdict

**PASS-with-triage.** There are no craft blockers. QA should execute the relevant test plan and test commands; the host should record CR-1 as tech debt before final task closure. CR-2 may be handled opportunistically.

**RECOMMENDED OUTCOME: pass**
