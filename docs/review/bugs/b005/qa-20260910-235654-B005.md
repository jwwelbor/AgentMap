# B005 QA report

## Verdict

**PASS** — B005 makes a successful non-text-only response visible on the
public rich receipt without projecting raw provider blocks into user-facing
text. The production caller path and the normalizer's status boundaries are
covered by the scoped unit suite.

## Scope and rationale

Reviewed the complete branch diff from `origin/main...HEAD` (7 paths, 338
added lines, 19 removed lines): task research, craft review, three production
paths, and two targeted unit-test modules.

| Path | QA disposition |
| --- | --- |
| `src/agentmap/models/llm_execution.py` | Additive, data-only `text_status` receipt contract reviewed. |
| `src/agentmap/services/llm/tool_call_extraction.py` | Reviewed the pure three-state response-content classifier and compatibility text projection. |
| `src/agentmap/services/llm_service.py` | Confirmed the classifier is reached from the successful async receipt construction path. |
| `tests/fresh_suite/unit/services/llm/test_tool_call_extraction.py` | Direct status-boundary coverage. |
| `tests/fresh_suite/unit/services/test_llm_service_async.py` | Production-shaped `call_llm_async()` caller-path coverage at the client-factory seam. |

The requested `docs/plan/bugs/B005.md`, feature `test-plan.md`, and feature
`spec.md` are not materialized in this worktree. QA used the task-scoped
`B005.research-report.md` and the committed craft review as the available
feature context. The research report records the intended receipt contract and
the explicit non-streaming boundary; this absence did not prevent verification
of the changed runtime behavior, but it is a traceability limitation.

No integration test was run: the change is localized to an intra-service
receipt normalization contract, and its consumer is exercised through the real
`LLMService.call_llm_async()` entrypoint. No streaming, API, database, or
cross-service seam changed.

## Checks and results

| Check | Result |
| --- | --- |
| `uv run black --check` on the 5 changed Python paths | PASS — 5 files unchanged. |
| `uv run isort --check-only` on the 5 changed Python paths | PASS. |
| `uv run flake8` on the 5 changed Python paths | PASS. |
| `uv run pytest --no-cov -q tests/fresh_suite/unit/services/llm/test_tool_call_extraction.py tests/fresh_suite/unit/services/test_llm_service_async.py` | PASS — 77 collected tests (16 direct-normalizer, 61 async-service tests), zero failures; 13 seconds including parallel quality gates. |
| Exploratory direct thinking-only block classification | PASS — `('', 'non_text')`; no reasoning content was exposed as text. |
| `git diff --check origin/main...HEAD` | PASS. |

## Acceptance and edge-case verification

| B005 contract | Evidence |
| --- | --- |
| A successful non-text-only response is distinguishable from ordinary empty text on the rich receipt. | `TestLLMServiceToolCallAndTextNormalizationWiring.test_b005_tool_use_only_response_is_explicitly_non_text` calls `LLMService.call_llm_async()` through the real client-factory seam and asserts `text == ""` plus `text_status == "non_text"`. |
| Existing structured tool-call information survives the receipt change. | The same caller-path test asserts the normalized `LLMToolCall` result while retaining a safe empty text projection. |
| Ordinary empty and visible text retain distinct statuses. | `TestNormalizeResponseContentStatus.test_b005_returns_the_expected_status_for_visible_and_empty_content` covers `""`, `[]`, an empty text block, and ordinary `"hello"`, expecting `empty` or `text` as appropriate. |
| Non-text block categories remain provider-neutral and do not leak sensitive content. | The direct exploratory thinking-only block check returned `('', 'non_text')`; the classifier uses absence of text blocks rather than provider-specific payload parsing. |

The scope also retains legacy malformed-block and plain-string regression
coverage in `test_tool_call_extraction.py`, and the service module retains the
live common text-path test. The craft-review boundary-coverage observation
CR-1 is resolved by the committed table-driven status test.

## Observation: repository-wide type baseline

`uv run mypy src` exited non-zero with **1,613 errors across 248 existing
source files**. An explicitly scoped invocation also cannot isolate B005
because mypy follows the service's existing import graph; it reported 655
errors across 82 files, including legacy annotation errors in unchanged areas
of `llm_service.py` and unrelated modules. Neither command reported an error
on B005's new classifier lines or `LLMResponse.text_status` addition.

This is a pre-existing repository-wide quality baseline, not a B005 regression
or a failed required format/lint gate. It should be tracked separately; it does
not block this localized release decision.

## Result

No B005 defects found. The scoped quality gates and all targeted tests passed;
the changed receipt contract is reachable from the production async entrypoint.

RECOMMENDED OUTCOME: pass

## Rework scope amendment

Later B005 rework added the `LLMAgent` and fan-out receipt consumers, removed
the dead compatibility wrapper, and added structured-content safety coverage.
The original five-path scope above describes the pre-rework QA snapshot; the
current B005 research report records the expanded sweep and its verification.
