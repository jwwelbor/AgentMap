# Code review — CC-005

**Reviewed:** 2026-09-11 12:57 CDT
**Review type:** craft review
**Scope:** `git diff HEAD~1` for `54f8612`: the research report, LLM service, and async-service test (257 added lines; no unrelated paths).
**Verdict:** **PASS**

## A. Executive summary

The change adds a debug-level, content-free diagnostic for successful `non_text` async LLM receipts and records evidence needed before a separate recovery mechanism can be selected. Raw provider blocks remain outside `LLMResponse.text` and the diagnostic.

Risk is low: the addition is a small service-local helper with no public signature, DI, factory, or streaming-path change. The task card deliberately defers a retry, raw-block API, or tool loop until a real occurrence is observed. Counts: **0 blockers / 0 non-blockers / 0 nits**. Independent red-team and complexity-tool notes are recorded below; neither produced a source finding.

## B. Findings table

No code or documentation findings.

## C. Reuse opportunities

The reuse pass searched `non_text`, `block_types`, and diagnostic helpers across `src`, `tests`, and planning documents. No competing block-type diagnostic exists. The implementation correctly reuses `normalize_response_content()` and `extract_tool_calls()` rather than re-parsing or exposing provider payloads.

## D. Standards crosswalk

No violations found. The implementation conforms to `claude.md`'s stated layering: business logic remains in `src/agentmap/services`, the DTO is not changed, and the unit test uses the established mock seam.

| finding id | standards doc | section | quoted clause |
| --- | --- | --- | --- |
| None | N/A | N/A | N/A |

## E. Tests review

The task card has no separately materialized feature `spec.md` or `test-plan.md`; its description is the available acceptance contract. Test execution is QA-owned and was not run in this craft review.

| task-contract item | covering test | counter-factual assessment |
| --- | --- | --- |
| Record type-only evidence when an async receipt is `non_text`. | `TestLLMServiceToolCallAndTextNormalizationWiring.test_cc005_non_text_receipt_logs_safe_block_type_diagnostic` at `tests/fresh_suite/unit/services/test_llm_service_async.py:1517` | It calls `call_llm_async()` through the client-factory seam and asserts the one diagnostic, block count, ordered block types, provider/model, request ID, finish reason, and usage presence. It fails if the helper is omitted, unreachable, or logs a different receipt shape. |
| Do not log raw provider content while observing its block types. | Same test at `tests/fresh_suite/unit/services/test_llm_service_async.py:1520-1567` | It supplies two secret-bearing provider blocks and asserts the secret is absent from emitted diagnostic arguments. A wrong implementation that passes raw blocks or block fields to the logger fails. |
| Do not select an automatic retry, raw-block API, or streaming behavior before direct evidence. | `docs/plan/changes/CC-005.research-report.md:97-133`; existing B005 receipt tests | This is a documented bounded-scope decision, not a new deterministic runtime branch. The reviewed diff adds no retry, DTO field, or streaming code; a contrary implementation would require edits outside this scope. |

The test is deterministic, has no time, randomness, network, or shared-state risk, and exercises the real service receipt path rather than merely calling the private helper. Its mock use is appropriate for the provider-client boundary.

## F. Quality rubric

| file | readability | maintainability | performance | testability | standards | notes |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `src/agentmap/services/llm_service.py` | 5 | 5 | 5 | 5 | 5 | The 40-line helper has one responsibility, derives only safe metadata, and adds linear work over the already-present block list. |
| `tests/fresh_suite/unit/services/test_llm_service_async.py` | 5 | 5 | 5 | 5 | 5 | The caller-path regression test checks diagnostic contents and secret exclusion. |
| `docs/plan/changes/CC-005.research-report.md` | 5 | 5 | 5 | 5 | 5 | It distinguishes implemented observation from the evidence-gated later recovery decision. |

## G. Risk hotspots

No new craft risk was found. The diagnostic reaches only the debug logger, uses content-block `type` labels rather than raw blocks, and preserves the existing safe-text receipt boundary. Manual inspection finds the new helper below the 50-line and cyclomatic-10 thresholds, and the changed diff adds no catch-all exception handling.

## G2. Codex red-team

Codex craft review was attempted twice in read-only mode. The first runner's inspection output exceeded the host capture limit before its final assessment could be retained. The permitted retry did not emit an agent final response before the host detached. The only verbatim retry output available was:

> `Reading additional input from stdin...`

No Codex findings were available to merge, downgrade, or disagree with. This is a non-blocking review-tool note, not a code finding; no `downgrade_justifications` apply.

The optional static complexity command `uv run radon cc src/agentmap/services/llm_service.py -s` could not run because `radon` is absent from the environment: `error: Failed to spawn: radon`. The manual threshold assessment above replaces that unavailable tool; no source change is requested.

## H. Production caller chains

No public service contract, protocol, factory, or DI registration changed, so the mandatory caller-chain gate is not triggered. The modified private receipt seam is nevertheless reachable through:

`LLMAgent.process_async()` at `src/agentmap/agents/builtins/llm/llm_agent.py:467` or `:483` → `LLMService.call_llm_async()` at `src/agentmap/services/llm_service.py:500-538` → async core/resilience dispatch → `_attempt_llm_call_async()` at `:1800-1869` → `_log_non_text_response_diagnostic()` at `:1871-1910` when `text_status == "non_text"`.

Production passes the agent's message list and either routing mode (`provider="auto"` plus routing context) or the agent's configured provider, model, and temperature. The helper receives the resolved provider and model, request ID if present, extracted finish reason, and whether normalized usage was present.

## I. Triage summary

- Blockers: none.
- Non-blockers to triage: none.
- Nits: none.
- Review-tool notes: the independent Codex assessment could not be retained after two read-only attempts, and `radon` is not installed. Neither issue produced a source-level concern.

## J. Verdict

**PASS.** The implementation is appropriately small, uses the existing receipt-normalization boundary, and does not select a billed or unsafe recovery policy without the observation the task card requires. QA should execute the named regression test and collect one controlled production occurrence before a separate recovery change is proposed.

RECOMMENDED OUTCOME: pass
