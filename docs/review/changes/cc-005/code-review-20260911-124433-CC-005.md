# Code review — CC-005

**Reviewed:** 2026-09-11 12:44 CDT
**Review type:** craft review
**Scope:** `git diff HEAD~1` (`acdfd7d`), which adds only `docs/plan/changes/CC-005.research-report.md`; inspected branch-context implementation in `0567a3c` solely to validate the report's claims.
**Verdict:** **FAIL (blocker)**

## A. Executive summary

The reviewed diff adds a well-structured research report for recovery from
non-text LLM receipts. It correctly preserves the existing safety boundary:
raw provider blocks must not enter `LLMResponse.text`.

The report's central decision is stale: the content-free diagnostic it
proposes is already implemented in the preceding branch commit, with a
caller-path regression test. It must describe that as implementation evidence,
not authorize it as new work. The requested CC-005 task card, feature spec,
and test plan are absent, so the recovery decision and its acceptance coverage
cannot be reviewed.

Overall risk: high for decision traceability; low for the documentation-only
diff's runtime impact. Counts: **2 blockers / 2 non-blockers / 0 nits**.

## B. Findings

| id | severity | file:line | rule | diagnosis | evidence | correction |
| --- | --- | --- | --- | --- | --- | --- |
| CR-1 | blocker | `docs/plan/changes/CC-005.research-report.md:88,95-103,122-130` | DRY | The report treats the metadata-only diagnostic as proposed/authorized work even though branch parent `0567a3c` has already implemented it. | `src/agentmap/services/llm_service.py:1845-1853` calls the diagnostic on every `non_text` receipt; `:1871-1910` emits exactly provider, model, request id, finish reason, usage presence, block count, and block types. | Rewrite the capability map and decision as implemented evidence. State that CC-005 now awaits a captured occurrence or a separately approved recovery mechanism; do not authorize already-completed work. |
| CR-2 | blocker | `docs/plan/changes/CC-005.research-report.md:28-31,129` | TESTS | The report acknowledges the absent change card but claims authority for a decision without the task specification, feature `spec.md`, or feature `test-plan.md` requested for this review. | `rg --files docs` found no CC-005 card, feature spec, or test plan. The report has no durable AC or test-case identifiers to trace. | Materialize or attach the three authoritative inputs, then map every CC-005 AC and test case to the recovery decision and tests. Until then, label the report a constrained implementation inventory, not an authorization. |
| CR-3 | non-blocker | `docs/plan/changes/CC-005.research-report.md:50-51,122-127` | TESTS | The report gives an untraceable aggregate claim that nine B005 tests passed and describes a diagnostic test as future work although it already exists. | `tests/fresh_suite/unit/services/test_llm_service_async.py:1517-1567` drives `call_llm_async()` through the client-factory seam, verifies all logged safe fields, and asserts that the provider secret is absent. No test was run in this craft review. | Name `test_cc005_non_text_receipt_logs_safe_block_type_diagnostic` and link a retained verification record for any pass count. Change the future-tense test plan to describe existing coverage and remaining gaps. |
| CR-4 | non-blocker | `docs/plan/changes/CC-005.research-report.md:143-145` | RISK | A material upstream-behavior claim relies on an absolute local UV-cache path. It exists in this environment but is not portable or durable repository evidence. | The cache source and adjacent metadata exist locally; `uv.lock` is the repository-owned dependency record. | Cite the package/version/hash in `uv.lock` as the durable source and label cache inspection as environment-specific supporting evidence. |

## C. Reuse opportunities

CR-1 is the material reuse finding. Reuse the existing receipt diagnostic:

- Existing capability: `LLMService._log_non_text_response_diagnostic()` at
  `src/agentmap/services/llm_service.py:1871`.
- Existing caller-path guard:
  `test_cc005_non_text_receipt_logs_safe_block_type_diagnostic()` at
  `tests/fresh_suite/unit/services/test_llm_service_async.py:1517`.
- Report-ready refactor: replace “Add a debug-level, content-free diagnostic”
  with “`0567a3c` adds the content-free diagnostic; collect one controlled
  occurrence before selecting a recovery policy.”

The reuse search (`rg 'block[_ -]?types|content[_ -]?free|response.*diagnostic|non_text' src tests docs/plan`) found this capability and no competing diagnostic utility.

## D. Standards crosswalk

No `coding-standards.md` was supplied or found. `claude.md` supplies
architecture and service-layer conventions but no documentation rule covering
these findings; its referenced `claude_documentation.md` is not materialized.
The blockers therefore rely on the review contract's required-input rule and
the concrete DRY/traceability risk, not invented project-standard citations.

| finding id | standards doc | section | quoted clause |
| --- | --- | --- | --- |
| CR-1 | Universal Technical Writing Style Guide | Core Philosophy | “Accurate” |
| CR-2 | Code Review workflow | Required inputs (validation) | “If any critical input is missing, request it before proceeding.” |
| CR-3 | Universal Technical Writing Style Guide | Quality Checklist — Accuracy | “Is every statement correct?” |
| CR-4 | No project standard covers it | opinion-only documentation portability concern | N/A |

## E. Tests review

Test execution is QA-owned and was not run in this craft review. Test design
inspection found one real diagnostic caller-path test at
`tests/fresh_suite/unit/services/test_llm_service_async.py:1517-1567`: it calls
the service through the client-factory seam, asserts every metadata field, and
proves provider payload content is not logged. That test would fail if the
diagnostic were omitted, emitted the wrong types/count, or logged the secret.

The report's statement that this verification remains to be added is stale.
The absent CC-005 task card and test plan prevent review of the full expected
case set, boundary cases, and TDD compliance. No flaky-risk or mock-discipline
finding was identified in the reviewed documentation diff itself.

### Counter-factual table

| AC | covering test | counter-factual assessment |
| --- | --- | --- |
| CC-005 ACs | Not assessable | **Blocker CR-2:** the acceptance criteria and feature test plan are not present, so no AC-to-test mapping can be made without inventing requirements. The existing diagnostic test is evidence for the report's proposed observation step, not proof of an unavailable recovery AC. |

## F. Quality rubric

| file | readability | maintainability | performance | testability | standards | notes |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `docs/plan/changes/CC-005.research-report.md` | 4 | 2 | 5 | 2 | 3 | Clear terminology and tables, but the stale central decision would duplicate already-implemented work; missing authoritative inputs prevent durable test traceability. |

The maintainability and testability scores are each 2, which confirms the
blocker verdict under the review rubric.

## G. Risk hotspots

- **Decision/wiring risk (blocker):** a report authorizing an already-wired
  diagnostic can cause duplicate implementation work while deferring the actual
  recovery decision.
- **Requirements risk (blocker):** missing task/spec/test-plan inputs leave no
  durable acceptance contract for a potentially billable retry or tool-loop
  recovery path.
- **Evidence portability (non-blocker):** an absolute cache path cannot be
  resolved by another checkout or CI worker.

## G2. Codex red-team

The independent read-only Codex review returned:

> 1. **Blocker** — `CC-005.research-report.md:88` and `:95` propose/authorize a diagnostic that already exists in the parent revision. `LLMService` logs the specified content-free fields for every `non_text` receipt at `llm_service.py:1845` and `:1871`. This is stale, duplicates an existing capability, and undermines the report’s DRY/reuse conclusion. Correction: describe it as implemented evidence, not new authorized work; focus CC-005 on interpreting collected observations or defining a separately approved recovery.
>
> 2. **Blocker** — `CC-005.research-report.md:28` acknowledges only the absent change card, while the requested feature spec and test plan are also absent. The report consequently cannot substantiate its claimed authority at `:129`, recovery scope, or acceptance/test obligations. Correction: mark the report as a constrained code inventory; materialize or attach durable authoritative inputs before approving any recovery design. Do not infer their requirements from B005 or `shark` output.
>
> 3. **Non-blocker** — `CC-005.research-report.md:50` reports “nine tests” without naming the selection, command, or durable result, and `:122` describes a future test that already exists: `test_llm_service_async.py:1517` verifies type-only diagnostics and secret exclusion. Correction: reference that exact test and its expected safe fields; replace the untraceable aggregate-pass claim with a retained verification record when available. No tests were run for this review.
>
> 4. **Non-blocker** — `CC-005.research-report.md:143` relies on an absolute UV-cache path for a material upstream claim. It exists in this environment and matches the locked 0.3.21 metadata, but it is not repository-portable. Correction: cite `uv.lock`’s version/hash as the durable source and identify the cache inspection as environment-specific supporting evidence.
>
> All cited repository paths and the cited cache files exist; `docs/plan/changes/CC-005.md`, `CC-005.feature-spec.md`, and `CC-005.test-plan.md` do not. The report otherwise follows the intended architecture direction: it reuses normalized `tool_calls`, preserves the data-only DTO boundary, avoids raw-block exposure, and keeps streaming separate.
>
> **Verdict:** changes requested. The missing authoritative inputs and stale central implementation claim prevent approval.

There is no disagreement: the red-team findings are incorporated as CR-1
through CR-4. No red-team blocker was downgraded.

## H. Production caller chains

Not applicable to the reviewed `HEAD~1` diff: it changes documentation only
and no service signature, factory, DI registration, or runtime caller. The
report's claimed diagnostic was nevertheless traced for accuracy:

`LLMService.call_llm_async()` → `_attempt_llm_call_async()` →
`normalize_response_content()` →
`_log_non_text_response_diagnostic()` at
`src/agentmap/services/llm_service.py:1829,1845-1853,1871-1910`.

The existing test passes a user message list and `provider="anthropic"` through
the client-factory seam at
`tests/fresh_suite/unit/services/test_llm_service_async.py:1535-1543`; this is
context validation only, not execution evidence from this review.

## I. Triage summary

### Blockers

- **CR-1:** Make the research report accurately distinguish already-implemented
  observation from the unselected recovery policy.
- **CR-2:** Provide the CC-005 task card, feature specification, and test plan;
  then trace recovery ACs and test cases to the decision.

### Non-blockers to triage

- **CR-3** — `docs/plan/changes/CC-005.research-report.md:50-51,122-127`:
  Replace the untraceable “nine tests passed” claim and stale future-test plan
  with the existing named diagnostic test and a retained verification record.
  **Fix:** cite the test and evidence artifact.
- **CR-4** — `docs/plan/changes/CC-005.research-report.md:143-145`:
  Replace the absolute UV-cache source with `uv.lock`-anchored evidence.
  **Fix:** retain cache inspection only as supplemental local evidence.

### Nits

- None.

## J. Verdict

**FAIL.** Correct CR-1 and CR-2 before QA. Re-review against the materialized
CC-005 acceptance contract; QA should then execute the mapped tests and
validate any chosen recovery path.

RECOMMENDED OUTCOME: fail
